'use strict';

/**
 * @ngdoc function
 * @name curtoTabuleiroApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the curtoTabuleiroApp
 */
angular.module('curtoTabuleiroApp')
  .controller('MainCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
